# Финальный аудит логики v1.4

## 1. Основной калькулятор господства

Исходная инструкция задаёт восемь этапов:

1. текущее господство и количество смотрителей;
2. Древо Конклава;
3. Бонус Вдохновения;
4. Кровь / уровень / таланты — до пяти основных смотрителей;
5. «Смотрители усиленные за книги» — значения «Бонус книг» у тех же пяти;
6. книги / Дегустация / доверие / прочее;
7. остальные смотрители и их таланты;
8. результат.

v1.4 повторяет именно эту последовательность, но переводит её на язык игровых действий.

## 2. Исправления формул основной книги

### Вдохновение Силы

`AE2` было структурно несогласовано с соседними суммами и давало завышение. Формула восстановлена по строкам источников Вдохновения и соседним атрибутам.

### Книги ×15

В `Z32:Z35` две последние группы должны считать количество × номинал × 15. Исходная формула ошибочно использовала количества `$X$32/$Y$32` как номиналы. Исправлено на константные ячейки номиналов `$X$30/$Y$30`.

### Суммы «Бонус книг»

`AG22` и `AG23` были формулами, а `AG24` и `AG25` — сохранёнными числами заполненного примера. Для универсального сайта это критично: чужие значения могли оставаться при новом игроке. Теперь `AG22:AG25` динамически суммируют пять видимых основных смотрителей с учётом единиц K/M/B.

### Новый контрольный результат

После исправлений заполненный сценарий даёт:

- `T72` = 86.465485126 M;
- `T73` = 361.325485126 M;
- `T15` = 62.44 M;
- `U12` = 4.994446 M;
- `Z37` = 11,693,185.5 raw;
- `T46` = 35,800;
- `U57` = 9,104,700;
- `T75` = 1996;
- `AO1` = 100.713022 B;
- `AS1` = 89.0439971 B;
- `AG24` = 5313.25 K;
- `AG25` = 5276.65 K.

## 3. Вдохновение

Текущий бонус Вдохновения используется как множитель прироста атрибутов основных смотрителей. Поэтому текущие бонусы уже открытых источников нельзя автоматически считать «необязательными» только потому, что игрок не планирует сегодня повышать Вдохновение.

UI v1.4 разделяет:

- «Бонус сейчас» — исходное состояние;
- «Добавлю» — планируемое изменение.

## 4. Пять основных смотрителей

Для каждого:

- имя;
- текущий уровень;
- целевой уровень;
- фактический Talent EXP;
- текущие уровни талантов;
- рукописи;
- гарантированные повышения;
- «Бонус книг» по четырём атрибутам.

Неиспользуемые слоты остаются пустыми и не участвуют в расчёте.

## 5. Таланты — отдельный калькулятор

`Калькулятор таланта.xlsx` задаёт пороги:

- ×6 → 1200;
- ×5 → 1000;
- ×4 → 800;
- ×3 → 600;
- ×2 → 400;
- ×1 → 200.

Формула рейтинга для выбранного коэффициента: `floor(EXP / порог) × коэффициент`.

Инструкция отдельно требует сначала распределить свитки по смотрителям, а затем вводить фактический EXP. Поэтому сайт не превращает нераспределённые свитки в рейтинг автоматически.

Рукописи ×1 учитываются как гарантированный вклад. Для ×2…×6 показывается только математическое ожидание — не гарантия.

## 6. Близость

Из `БлизостьВлечениеРесурсы.xlsx`:

Фиксированные: +1 / +2 / +5 и монеты +1 / +2 / +5 / +25.

Случайные:

- Сумочка 1–4;
- Мешок 5–8;
- Шкатулка 4–50.

Первая копия листа содержит ошибку: формула минимального итога пропускает минимум Шкатулки. Две другие копии включают его. v1.4 использует согласованную исправленную логику.

Средняя оценка = `(минимум + максимум) / 2`, как в исходной книге.

## 7. Влечение

Фиксированные букеты: +1 / +2 / +5.

Фиксированные записки случайному партнёру: +1 / +2 / +5 / +25.

Случайные:

- Пиала 1–4;
- Бутыль 5–8;
- Шкатулка 4–50.

Минимум, максимум и среднее совпадают со структурой всех трёх исходных копий.

## 8. Сбор ресурсов

Формулы исходного листа:

- жетон 1: сбор × 0.1;
- жетон 2: × 0.5;
- жетон 3: × 1;
- жетон 4: × 2;
- жетон 5: × 3;
- жетон 6: × 5;
- жетон 7: × 10;
- карта сбора: × 1.

Сайт применяет эту механику к Крови, Нектару и Летучим мышам, поддерживая K/M/B.

## 9. Принцип точности

- «Точно» — формула/фиксированный предмет.
- «Оценка» — случайный диапазон или вероятность.
- «Совет сообщества» — стратегия, не формула.

Эти категории не смешиваются в одном гарантированном результате.
